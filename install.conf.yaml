- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf:
    ~/.tmux.line.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
      force: true

# Additional tools
    ~/.latexmkrc:
    ~/.config/htop/htoprc:

# Git-related
    ~/.gitconfig:
    ~/.gitattributes_global:
    ~/.gitignore_global:

# Collection of default applications
    ~/.duti:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - ["curl https://raw.githubusercontent.com/Shougo/dein.vim/master/bin/installer.sh > .dein-installer.sh", Downloading vim package manager]
  - ["sh ./.dein-installer.sh ~/.vim/bundle", Installing vim package manager]
