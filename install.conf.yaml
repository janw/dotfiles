- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.latexmkrc: latexmkrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
